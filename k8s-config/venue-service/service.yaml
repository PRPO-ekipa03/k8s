apiVersion: v1
kind: Service
metadata:
  name: venue-service
  labels:
    app: venue-service
spec:
  selector:
    app: venue-service
  ports:
    - protocol: TCP
      port: 8084
      targetPort: 8084
